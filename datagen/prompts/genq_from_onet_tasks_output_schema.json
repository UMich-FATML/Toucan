{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "O*NET Task Tool-Use Output Schema",
  "description": "Structured output schema for prompt template prompts/genq_from_onet_tasks.md.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "tool_analysis",
    "cross_tool_workflow",
    "target_tools",
    "request"
  ],
  "properties": {
    "tool_analysis": {
      "type": "string",
      "minLength": 1,
      "description": "Analysis of how selected tools support the provided O*NET tasks."
    },
    "cross_tool_workflow": {
      "type": "string",
      "minLength": 1,
      "description": "Workflow across tools including dependencies and decision points."
    },
    "target_tools": {
      "type": "array",
      "minItems": 1,
      "description": "Ordered tool calls required to satisfy the request.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "server",
          "tool",
          "arguments",
          "output"
        ],
        "properties": {
          "server": {
            "type": "string",
            "minLength": 1,
            "description": "MCP server name that provides the tool."
          },
          "tool": {
            "type": "string",
            "minLength": 1,
            "description": "Tool name on the selected server."
          },
          "arguments": {
            "type": "object",
            "description": "Tool call arguments matching the tool input schema.",
            "additionalProperties": true
          },
          "output": {
            "description": "Expected output from executing the tool call."
          }
        }
      }
    },
    "request": {
      "type": "string",
      "minLength": 1,
      "description": "Natural language user request grounded in the O*NET tasks."
    }
  }
}
